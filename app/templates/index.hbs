<form role="form">
  <div class="form-group">
    <label for="rate">Interest Rate</label>
    {{input class="form-control" value=rate placeholder="Enter Interest Rate"}}
  </div>
  <div class="form-group">
    <label for="rate">Amount Borrowed</label>
    {{input class="form-control" value=amount placeholder="Enter The Amount You're Borrowing"}}
  </div>
  <div class="form-group">
    <label for="rate">Enter The Loan Term</label>
    {{input class="form-control" value=term placeholder="Enter The Term In Months"}}
  </div>
</form>

{{#if isTyping}}
  You are typing...
{{else}}
  {{#unless isValid}}
    Waiting on valid input...
  {{/unless}}
{{/if}}
<br/>
Payment amount: {{monthlyPayment}}
